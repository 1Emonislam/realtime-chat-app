import { Typography } from '@mui/material';
import React from 'react';

const ChatBodyPage = () => {
    return (
        <div>
        <Typography>
            one
        </Typography>
        </div>
    );
};

export default ChatBodyPage;